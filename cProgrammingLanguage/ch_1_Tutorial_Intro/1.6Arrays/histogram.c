#include <stdio.h>
main()
{
	int c, inspace;
	int word[i];

	inspace = 0;
	while((c = getchar()) != EOF)
		if(c == ' ' || c == '\n' || c == '\t')
		{
			inspace = 0;
			printf("\n");
		}
		else
		{
			putchar(c);
		}

					
		
}
	
